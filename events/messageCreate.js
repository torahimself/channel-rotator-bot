module.exports = {
  name: 'messageCreate',
  async execute(message) {
    // This event is needed for the interaction system to work
    // Most logic is handled in interactionCreate.js
    return;
  },
};
